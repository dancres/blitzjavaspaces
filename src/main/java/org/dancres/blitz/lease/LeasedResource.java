package org.dancres.blitz.lease;

/**
   Marker interface - if a class does not implement this interface, it cannot
   be subject to filtering via ReapFilter instances.

   @see org.dancres.blitz.lease.ReapFilter
 */
public interface LeasedResource {
}
